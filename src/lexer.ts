interface Token {
    type: string,
    data: string
}

function tokenize(source: string): Token[] {
    let src = source.split(""); //all the chars 

    while (src.length > 0){

        /* switch(src){
            case "a" break;
        } */

    }

    return /* tokens */
}
