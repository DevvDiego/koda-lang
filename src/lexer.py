from models.tokenType import TokenType


def buildToken(type:TokenType, value:str):
    """
    Builds a standarized token
    Parameters:
        type (str): Token type (Listed in TokenTypes dictionary).
        value (str): The value that the token stores.

    Returns:
        int: Sum of a and b.
    """


buildToken(TokenType.PLUS,"+");